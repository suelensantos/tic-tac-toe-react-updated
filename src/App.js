// According to the new documentation, all functions (components) are represented
// directly in this App file. I separated them into different component files.

import Game from "./components/Game";

const App = () => <Game />;

export default App;